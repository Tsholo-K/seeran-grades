import Settings from "@/components/(settings components)/settings";


const SettingsPage = () => {
  return (
    <div>
      <Settings dashboard={'parent'} />
    </div>
  )
};

export default SettingsPage;