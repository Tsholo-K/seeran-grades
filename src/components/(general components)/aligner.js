'use client';

import { useEffect } from "react";

const Aligner = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
      }, [])

    return (
        <div></div>
    )
};

export default Aligner;